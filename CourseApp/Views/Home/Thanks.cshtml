@model Request
@{
    ViewData["Title"] = "Thanks";
}

<div class="text-center">
    <h1>Thank you @Model.Name</h1>
    <h4>Our Course is going to be nice.</h4>
    <a asp-action="List" class="btn btn-primary">Check who are coming.</a>
</div>

